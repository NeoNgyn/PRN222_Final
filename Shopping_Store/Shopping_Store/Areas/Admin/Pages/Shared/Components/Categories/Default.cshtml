@model IEnumerable<Shopping_Store_Data.Model.Category>

@foreach (var item in Model)
{
	<div class="panel panel-default">
		<div class="panel-heading">
			<h4 class="panel-title"><a asp-page="/Categories/Index" asp-route-slug="@item.Slug">@item.Name</a></h4>
		</div>
	</div>
}